{
  "name": "rebuzz-ai-service",
  "version": "0.1.0",
  "private": true,
  "description": "Stores each business's own AI provider key (Gemini or OpenRouter) and makes model calls on their behalf.",
  "main": "app.js",
  "scripts": {
    "dev": "node --watch --env-file=.env app.js",
    "start": "node --env-file=.env app.js"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^6.13.5"
  },
  "engines": {
    "node": ">=22"
  }
}
